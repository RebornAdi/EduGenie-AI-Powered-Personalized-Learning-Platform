# EduGenie Frontend

React + Vite client for EduGenie.

Use the root npm scripts for normal development:

```bash
npm run setup
npm run dev
```

Frontend-only commands are still available:

```bash
npm run dev:frontend
npm run build
npm run lint
```
